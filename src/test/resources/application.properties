spring.datasource.url=jdbc:h2:mem:piggyback-test;MODE=MySQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

abuba.auth.jwt-secret=test-only-jwt-secret-key-at-least-32-characters
abuba.auth.expose-mock-code=true

piggyback.integrations.llm.base-url=https://llm.invalid/v1
piggyback.integrations.llm.api-key=test-only
